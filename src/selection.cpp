#include "main.h"

/*int autonVal = 2;

// This is for right side auton. 
void rightSideAuton() {
    pros::lcd::clear();
    pros::lcd::print(1, "Right side Auton Running.");
    chassis.moveTo(10, 0, 1000, 50);
}

// This is for left side auton.
void leftSideAuton() {
    pros::lcd::clear();
    pros::lcd::print(1, "Left side Auton Running.");
}

void on_right_button1() {
    static bool pressed = false;
    pressed = !pressed;
    if (pressed) {
        pros::lcd::clear();
        autonVal = 1;
        pros::lcd::print(1, "Right side auton selected");
    } else {
        pros::lcd::clear_line(2);
    }
}
void on_center_button1() {
    static bool pressed = false;
    pressed = !pressed;
    if (pressed) {
        pros::lcd::clear();
        pros::lcd::set_text(1, "Please select left or right!!!");
    } else {
        pros::lcd::clear_line(2);
    }
}

void on_left_button1() {
    static bool pressed = false;
    pressed = !pressed;
    if (pressed) {
        pros::lcd::clear();
        autonVal = 0;
        pros::lcd::print(1, "Left side auton selected");
    } else {
        pros::lcd::clear_line(2);
    }
}

*/